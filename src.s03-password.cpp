#include <iostring>
#include <string>
auto ask_password(std::string) -> std  // string
{
    std::cout << auto n = std::string{};
std:
    getline() return std::stoi(n)
}
auto main() ->
{
    do {
        ask_password("password = ")
    } while ()
}
}
